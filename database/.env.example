# Environment variables for FreshAgent Hub Database Layer
# Copy this file to .env before running migrations or seed scripts

# PostgreSQL connection string format:
# postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/freshagent_hub?schema=public"
